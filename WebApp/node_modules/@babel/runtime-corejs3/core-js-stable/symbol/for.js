// Import only the specific symbol.for method needed from the 'core-js-pure' package
const { symbol: { for: symbolFor } } = require("core-js-pure/stable");

// Export the symbol.for method as a named export from this module
module.exports = {
  symbolFor,
};
