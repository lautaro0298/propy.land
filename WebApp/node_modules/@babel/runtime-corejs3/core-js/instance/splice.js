// Import only the specific splice method from the 'core-js-pure' module,
// and alias it as `splice` for convenience.
const { splice: _splice } = require("core-js-pure/stable");

// Export the aliased `splice` method as the default export of this module.
module.exports = _splice;
