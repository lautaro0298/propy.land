// Importing only the specific Reflect method needed
const { getOwnMetadataKeys } = require("core-js-pure/stable");

// Exporting the function so it can be used in other modules
module.exports = getOwnMetadataKeys;
