// Import only the specific symbol.split method needed from the 'core-js-pure' package
const { split: symbolSplit } = require("core-js-pure/stable");

// Create a new named export for the symbol.split method
module.exports = {
  symbolSplit,
};
