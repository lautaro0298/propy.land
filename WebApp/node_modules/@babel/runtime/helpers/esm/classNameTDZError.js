export default function classTDZError(name) {
  const error = new Error();
  error.name = 'ClassReferenceError';
  error.message = `Class "${name}" cannot be referenced in computed property keys.`;
  throw error;
}
