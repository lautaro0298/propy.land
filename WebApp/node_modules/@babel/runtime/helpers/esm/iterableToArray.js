export default function iterableToArray(iter) {
  if (Array.isArray(iter)) return iter;
  if (typeof Symbol === 'undefined' || !iter[Symbol.iterator]) throw new TypeError('Expected Iterable');
  return [...iter];
}
