@model  List<LibreriaClases.DTO.DTOTipoPublicacion>
@{

    ViewBag.Title = "Eliminar Tipo Publicacion";
    Layout = "~/Views/Shared/MasterPanelAdministrador.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <div>
        <table class="table table-dark table-hover">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Tipo de Publicacion</th>
                    <th scope="col">Activo</th>

                </tr>
            </thead>
            <tbody>
                @for (int m = 0; m < Model.Count; m++)
                {
                    <tr>
                        <th scope="row">@m</th>
                        <td>@Model.ElementAt(m).nombreTipoPublicacion</td>
                        <td></td>
                        <td><form action="/ABMPublicacion/EliminarPublicacion" method="post"><input type="hidden" name="id" value="@Model.ElementAt(m).tipoPublicacionId" /><button class="btn btn-danger" type="submit">Eliminar</button></form></td>
                    </tr>

                }



            </tbody>

        </table>

    </div>
</body>
</html>
